/**
 * This program demonstrate a sample use case
 * of arrays in rust.
 * Note: All the elements should have the same
 * data-type.
 */
fn main() {
  let words = ["this", "community", "great", "is", "but", "have", "great", "courage", "dedication", "programming", "requires", "and"];

  print!("{} {} {} {}", words[0], words[9], words[3], words[6]);
}