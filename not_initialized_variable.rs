/**
 * This program won't be compile and will throw
 * compilation error because we didn't initialize
 * the variable 'number' by assigning value to it.
 */
fn main() {
  let number;
  print!("{}", number);
}